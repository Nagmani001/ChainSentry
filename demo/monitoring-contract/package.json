{
  "name": "chainsentry-monitoring-contract-demo",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.mjs --network sepolia",
    "traffic": "hardhat run scripts/send-traffic.mjs --network sepolia",
    "incident": "hardhat run scripts/trigger-incident.mjs --network sepolia",
    "evaluate": "node scripts/evaluate-alert.mjs"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "dotenv": "^17.4.2",
    "ethers": "^6.15.0",
    "hardhat": "^2.27.0"
  }
}
